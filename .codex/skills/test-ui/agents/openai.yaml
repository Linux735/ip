interface:
  display_name: "UI Test Runner"
  short_description: "Run and verify console UI and save/load test cases"
  default_prompt: "Use $test-ui to run the project's console UI and save/load test plan."
